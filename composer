#!/usr/bin/env bash

# Wrapper script for running composer commands in Docker
# Usage: ./composer install
#        ./composer require package/name

if [ ! -f docker-compose.yml ]; then
    echo "Error: docker-compose.yml not found. Run from project root."
    exit 1
fi

docker compose exec web composer "$@"
